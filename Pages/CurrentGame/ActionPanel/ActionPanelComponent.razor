@using Transdim.DomainModel.GameComponents;
@using Transdim.Pages.CurrentGame.ActionPanel.PowerAction.Modal;


@inject Transdim.Service.IGameStateService GameStateService;
@inject Blazored.Modal.Services.IModalService ModalService;
@inject Transdim.Utilities.UiQueueExecutor UiQueueExecutor;

<div class="row">
    <GameComponentIcon onclick="@(() => PerformAction(GameComponents.ActionPowerAction))" gameComponent="@GameComponents.ActionPowerAction"></GameComponentIcon>
</div>


@functions {
    private void PerformAction(IGameComponent gameComponent)
    {
        // TODO: replace with a call to a componentActivator that does all this
        UiQueueExecutor.Add(typeof(PowerActionModal));
        UiQueueExecutor.ExecuteQueue();
    }
}