@using Transdim.DomainModel.GameComponents;
@inject Transdim.Service.IGameComponentController GameComponentController;

<img class="tech-tile" onclick="@PerformAction" src="@component.ImagePath" asp-append-version="true" />

@functions {
    [Parameter]
    private IGameComponent component { get; set; }

    [Parameter]
    private Action OnClickAddition { get; set; }

    private void PerformAction()
    {
        OnClickAddition?.Invoke();
        GameComponentController.Activate(component);
    }
}